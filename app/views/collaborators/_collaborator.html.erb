<div class="media">
  <div class="media-body">
    <p><strong><%= collaborator.user.email %></strong> - <%= collaborator.user.email %> <%= link_to "Remove", [collaborator.wiki, collaborator], method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to remove this collaborator?'} %>
  </div>
</div>
